
For in => in = index/objects 
For in loop is used on objects which will return the keys of a object and for arrays they return array indexes not array values

For of => of = values of array, string, maps
For of is used on arrayes which will return the values of an array.
